// include necessary headers
#include <iostream>  // Standard library for input/output

// Use explicit namespace qualification (Avoid using `using namespace std;`)
int main() {
    // Properly document the purpose of the program
    std::cout << "Hello, World! Welcome to C++ Mastery!" << std::endl;  // Print a message
    
    return 0;  // Explicitly return 0 for successful execution
}
